interface:
  display_name: "Check"
  short_description: "Review code changes before shipping"
  default_prompt: "Use $check to review this change with functional acceptance, adversarial review, verification, conditional Sealos deployment audit, and release-readiness gates."
